// ShaderLabel.js

class ShaderLabel {
	constructor({ id, offset, length, name }) {
		this.id = id;
		this.offset = offset;
		this.length = length;
		this.name = name;
	}
}

module.exports = { ShaderLabel };